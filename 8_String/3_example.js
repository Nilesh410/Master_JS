/* Step 1: Create a variable using const and assign ⇒ “ Horray! ”.

Step 2: Now print ⇒ index of “r” inside the string.

Step 3: Remove the extra spaces around “ Horray! ” ⇒ “Horray!” using a string method and directly print it to the console. */

const name1=" Horray! "
console.log(`Index of r is ${name1.indexOf('r')}`)
console.log(name1.trim())