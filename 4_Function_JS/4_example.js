// For function not only primitive data types value we can pass, we can pass no primitive values also
// We can pass object and array as argument to the function 

function display(data)
{
    console.log(data)
}

display({name:"Nilesh",
    rollno:22101
})
display([10,20,30])
